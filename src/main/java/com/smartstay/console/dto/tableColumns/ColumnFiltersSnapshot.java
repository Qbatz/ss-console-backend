package com.smartstay.console.dto.tableColumns;

public record ColumnFiltersSnapshot(int order,
                                    String fieldName,
                                    boolean isSelected) {
}
