package com.smartstay.console.responses.hostels;

public record HostelFilterOptionsRes(String key,
                                     String label,
                                     long count) {
}
